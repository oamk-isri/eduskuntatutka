import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  // Header.js

  header: {
    height: 75,
  },
  logo: {
    height: 50,
    width: 35,
    margin: 8,
  },

  // RootNavigator.js

  title: {
    fontSize: 25,
    alignSelf: "center",
    marginTop: -30,
    marginBottom: 15,
  },

  titleStyle: {
    textAlign: "center",
  },

  // StackNavigators.js

  // -> nothing to style!
});
